import os
import sys

def main():
    print("Models are now pre-packaged in the repo (.onnx and .tflite).")
    print("No need to download models at runtime anymore.")
    print("Models successfully cached!")

if __name__ == "__main__":
    main()
